name: Keep Render awake
on:
  schedule:
    - cron: '*/10 * * * *'  # каждые 10 минут
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: curl -I https://my-translator-bot-gt7b.onrender.com/health
